import { lightTheme } from "@rainbow-me/rainbowkit";

const base = lightTheme({
  accentColor: "#1B5E20",
  accentColorForeground: "white",
  borderRadius: "large",
  fontStack: "system",
});

export const katangaRainbowTheme = base;
